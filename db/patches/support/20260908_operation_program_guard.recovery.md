# Recovery — operation programme guard

Additive function replacement, no business data changes. Back up before applying, capture the previous function definition with the preflight, then run the verification. The material workflow still verifies readiness in its locked transaction; this patch scopes the older SQL programme backstop to the frozen operation type. Unknown operations and machining retain the programme block. An OF without an active dossier proof retains its legacy rule.

To revert after stopping new starts, replace only `fn_guard_preparation_execution_712` with the definition captured before applying (also in `20260905_production_preparation_consolidation_03.sql`). Do not replay that entire old migration or drop tables/triggers. Existing quantities, pointages and stock are preserved. Restoring a database backup needs a separate recovery decision.
